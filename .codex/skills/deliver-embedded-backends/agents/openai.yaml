interface:
  display_name: "Maintain Embedded Backend Fork"
  short_description: "Rebase and validate the embedded Buzz fork"
  default_prompt: "Use $deliver-embedded-backends to rebase upstream beneath the fork, resolve conflicts, run compatibility gates, and publish origin/main."
